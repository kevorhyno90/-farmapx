<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>Add Crop</title>
    <link rel="stylesheet" href="/css/style.css">
</head>
<body>
    <%- include('../partials/navbar') %>
    <div class="container">
        <h1>Add New Crop</h1>
        <% if (errors.length > 0) { %>
            <div class="errors">
                <ul>
                    <% errors.forEach(function(error) { %>
                        <li><%= error.msg %></li>
                    <% }); %>
                </ul>
            </div>
        <% } %>
        <form action="/crops/add" method="POST">
            <label for="name">Name:</label>
            <input type="text" id="name" name="name" value="<%= locals.crop ? crop.name : '' %>" required>

            <label for="planted_date">Planted Date:</label>
            <input type="date" id="planted_date" name="planted_date" value="<%= locals.crop ? crop.planted_date : '' %>" required>

            <label for="expected_harvest">Expected Harvest Date:</label>
            <input type="date" id="expected_harvest" name="expected_harvest" value="<%= locals.crop ? crop.expected_harvest : '' %>" required>

            <label for="status">Status:</label>
            <select id="status" name="status">
                <option value="Growing" <%= (locals.crop && crop.status === 'Growing') ? 'selected' : '' %>>Growing</option>
                <option value="Harvested" <%= (locals.crop && crop.status === 'Harvested') ? 'selected' : '' %>>Harvested</option>
                <option value="Planned" <%= (locals.crop && crop.status === 'Planned') ? 'selected' : '' %>>Planned</option>
            </select>

            <button type="submit">Add Crop</button>
        </form>
    </div>
</body>
</html>
